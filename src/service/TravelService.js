const Travels = require("../models/Travels");

const ShowTravel = async (
  departure_point,
  arrival_point,
  depart_date,
  arrival_date,

  can_take,
  lowest_price,
  verify_profile,
  max_number_ofday
) => {
  new Promise(async (resolve, reject) => {
    try {
    } catch (error) {
      reject(error);
    }
  });
};
